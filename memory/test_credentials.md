# Test Credentials

## Authentication System
The app uses anonymous tokens (no username/password login).

### How to authenticate:
POST /api/auth/anonymous with {"role": "particulier"} returns a token.

### Note about marc19/Solerys777!
These credentials were mentioned by the user but the system uses anonymous tokens only.
No username/password login exists. Users click "Accéder" on the landing page.
